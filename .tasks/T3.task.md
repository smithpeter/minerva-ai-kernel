# Task: Implement Policy Runtime v0 and dangerous action tests

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/5
- Owner Lane: Security
- Allowed Files: `minerva_kernel/`, `tests/`, `docs/`, `policies/`

## Goal

Create the first hard safety boundary between model decisions and any executable action.

## Acceptance Criteria

- Default policy is read-only.
- Dangerous action labels or tools are blocked with explicit reasons.
- Tests cover destructive command attempts, credential access attempts, writes, and unknown actions.
- CLI output can show policy decision reason.

## Non-Goals

- Do not implement auto-repair.
- Do not allow arbitrary model-generated shell commands.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
```

## Output

Implemented Policy Runtime v0 in `minerva_kernel/policy.py` with default read-only behavior.
Blocked dangerous action labels/tools, destructive command attempts, credential access attempts, write attempts, missing actions, and unknown actions with explicit reasons.
Added `minerva policy-check <decision.json>` CLI output showing allowed/blocked status and policy reason without executing payloads.
Added tests in `tests/test_policy.py` and updated `tests/test_cli.py` to cover dangerous commands, credential access, writes, dangerous labels/tools, unknown actions, and CLI block reasons.
Documented Runtime v0 behavior in `policies/README.md`.

Verification:
- `python3 -m unittest discover tests` passed: 18 tests.
- `python3 -m compileall minerva_kernel` passed.
