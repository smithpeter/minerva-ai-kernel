# Task: Expand labeled failure cases to at least 100

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/14
- Owner Lane: Eval
- Allowed Files: `evals/`, `tests/`, `docs/`, `taxonomies/`, `minerva_kernel/`

## Goal

Grow the M0 labeled failure corpus from the first slice to at least 100 validated cases.

## Acceptance Criteria

- Corpus contains at least 100 validated labeled cases.
- Cases cover Python, shell/CLI, git, npm/node, Docker/build, DNS/network, permissions, timeout, CI, model/API, schema/JSON, and secret-redaction categories.
- Loader validation catches duplicate IDs and malformed expected fields.
- Documentation summarizes category counts and contribution target.
- Existing smoke eval remains fast and still passes.

## Non-Goals

- Do not require network, Docker, npm, package installs, or remote services.
- Do not benchmark real local models.
- Do not include real secrets, private logs, proprietary code, or full environment dumps.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
python3 -m minerva_kernel.eval_smoke
```

## Output

- Confirmed git root: `/Users/zouyongming/projects/minerva-ai-kernel`.
- Expanded `evals/failure_cases_m0_slice.json` from 24 to 100 validated synthetic cases.
- Category counts: `python` 8, `shell-cli` 8, `git` 9, `npm-node` 8, `docker-build` 8, `dns-network` 8, `permission` 8, `timeout` 8, `ci` 9, `model-api` 9, `schema-json` 9, `secret-redaction` 8.
- Added required M0 category validation for `git`, `ci`, `model-api`, and `schema-json`.
- Added loader duplicate-ID validation and unit coverage for duplicate IDs and malformed expected fields.
- Updated evaluation/contribution docs with category counts and the next contribution target.
- Ran required Test / Eval commands successfully:
  - `python3 -m compileall minerva_kernel`
  - `python3 -m unittest discover -s tests` (`50` tests passed)
  - `python3 -m minerva_kernel.eval_smoke` (`5/5` smoke cases passed)
