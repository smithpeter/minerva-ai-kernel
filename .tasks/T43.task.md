# Task: Harden read-only executor path boundaries

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/43
- Owner Lane: Security / Kernel
- Allowed Files: `minerva_kernel/`, `tests/`, `docs/`, `.tasks/`

## Goal

Strengthen the explicit read-only executor so diagnostic handlers cannot inspect
paths outside the requested project root or sensitive credential locations.

## Acceptance Criteria

- Executor path candidates are normalized relative to `cwd`.
- `..` escapes, sensitive credential paths, and absolute paths outside `cwd` are
  rejected with a bounded executor observation.
- Existing handlers remain read-only and do not run shell commands.
- Unit tests cover allowed relative paths, path escape rejection, and credential
  path rejection.

## Non-Goals

- Do not add write or repair actions.
- Do not read arbitrary file contents.
- Do not weaken policy runtime defaults.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
```

## Output

Completed executor path-boundary hardening.

- Normalized executor path candidates relative to `cwd`.
- Rejected path escapes such as `../outside.txt`.
- Rejected credential-like paths such as `.env`.
- Preserved read-only behavior: no shell commands, writes, installs, or repairs.
- Added unit tests for path escape and credential-like path rejection.
