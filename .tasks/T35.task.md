# Task: Fix package metadata so editable install exposes minerva CLI

- Status: pending
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/35
- Owner Lane: Release / Integration
- Allowed Files: `pyproject.toml`, `tests/`, `docs/`, `README.md`, `CHANGELOG.md`, `.tasks/`

## Goal

Fix the package metadata blocker recorded by the local release dry run so a
fresh virtualenv can install the project and expose the documented `minerva`
console command.

## Acceptance Criteria

- Update packaging metadata so setuptools installs only the intended Python package(s) and does not treat `evals`, `models`, `policies`, `adapters`, or `taxonomies` as top-level packages.
- Prefer modern SPDX license metadata if it avoids current setuptools deprecation warnings.
- Add or update tests that verify `python -m pip install --no-deps --no-build-isolation -e .` or an equivalent local editable install exposes `minerva doctor` from a fresh virtualenv without network access.
- Update release readiness or dry-run docs to mark the previous blocker fixed if verification passes.
- Existing compile and unit tests pass.

## Non-Goals

- Do not publish packages.
- Do not download dependencies.
- Do not download models.
- Do not create external accounts.
- Do not use VoxSign state, tasks, tools, or docs.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
```

## Output

Pending.
