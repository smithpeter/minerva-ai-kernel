# Task: Add GitHub Actions CI smoke workflow

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/10
- Owner Lane: Integration
- Allowed Files: `.github/`, `docs/`, `README.md`

## Goal

Add a minimal GitHub Actions workflow so every push/PR runs the same local checks used by the AI team.

## Acceptance Criteria

- Add a workflow under `.github/workflows/`.
- CI runs on push and pull_request.
- CI uses Python 3 and runs:
  - `python -m compileall minerva_kernel`
  - `python -m unittest discover -s tests`
  - `python -m minerva_kernel.eval_smoke`
- Documentation mentions the CI gate.

## Non-Goals

- Do not add package publishing.
- Do not require external services, GPUs, Ollama, Docker, or remote LLM credentials.
- Do not change runtime code unless needed to make CI import paths work.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
python3 -m minerva_kernel.eval_smoke
```

## Output

- Added `.github/workflows/ci-smoke.yml` to run on `push` and `pull_request`.
- Workflow uses Python 3 and runs `python -m compileall minerva_kernel`, `python -m unittest discover -s tests`, and `python -m minerva_kernel.eval_smoke`.
- Documented the CI smoke gate in `README.md`.
- Verified locally:
  - `python3 -m compileall minerva_kernel` passed.
  - `python3 -m unittest discover -s tests` passed: 46 tests.
  - `python3 -m minerva_kernel.eval_smoke` passed: 5/5 cases.
