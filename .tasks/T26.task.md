# Task: Add ecosystem contribution docs for packs and adapters

- Status: pending
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/26
- Owner Lane: Docs / Ecosystem
- Allowed Files: `docs/`, `examples/`, `README.md`

## Goal

Make contribution paths explicit for taxonomy packs, policy packs, model packs,
and optional adapters.

## Acceptance Criteria

- Document the expected contents and safety requirements for taxonomy packs,
  policy packs, model packs, and adapters.
- Explain ownership boundaries between core Minerva, optional integrations, and
  community-contributed packs.
- Preserve the M0/M1 claims: no auto-repair by default, redaction before model
  input, policy-gated decisions, and CPU-local minimum path.
- Link the ecosystem contribution guide from README or an existing docs index.
- Existing compile and unit tests pass.

## Non-Goals

- Do not create external accounts or publish packages.
- Do not implement every adapter.
- Do not use VoxSign state, tasks, tools, or docs.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
```

## Output

Pending.
