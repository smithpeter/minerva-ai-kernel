# Task: Add Minerva public site artifact and domain cutover runbook

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/41
- Owner Lane: Release / Docs
- Allowed Files: `public-site/`, `docs/`, `README.md`, `CHANGELOG.md`, `tests/`, `.tasks/`

## Goal

Prepare a standalone Minerva public page artifact and a precise domain cutover
runbook so `minervakernel.com` can be moved away from the current VoxSign
certificate/page without ambiguity.

## Context

Readiness for commit `b540e215fc193321b5b6159be120218cbd992aee` is green for
local install backend, GitHub Actions, and DNS. The remaining blocker is
external: `minervakernel.com` resolves but TLS fails because the served
certificate is for `test.voxsign.net`, and the HTTPS page content is VoxSign.

## Acceptance Criteria

- Add a standalone static public page artifact for Minerva that can be served by
  nginx or any static host.
- The page must clearly present Minerva AI Kernel, the narrow M0 claim,
  docs/GitHub links, safety non-claims, and the CPU-local failure interpreter
  positioning.
- Add a domain cutover runbook with nginx/static-host steps, certificate
  requirements, rollback notes, and readiness verification commands.
- Update release readiness/docs/changelog to point to the artifact and runbook.
- Add lightweight tests/checks where practical to prevent VoxSign markers in the
  public artifact.

## Non-Goals

- Do not deploy to the server.
- Do not modify VoxSign files or workflows.
- Do not claim the domain is fixed until readiness passes with valid TLS and
  Minerva content.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
```

## Output

- Confirmed git root before editing:
  `/Users/zouyongming/projects/minerva-ai-kernel`.
- Added standalone static Minerva public page artifact:
  `public-site/index.html`.
- Added domain cutover runbook:
  `docs/minervakernel-domain-cutover-runbook.md`, covering nginx,
  managed static hosts, certificate requirements, DNS cutover, rollback, and
  readiness verification commands.
- Updated `README.md`, `CHANGELOG.md`,
  `docs/m0-release-readiness.md`, and
  `docs/m0-ci-domain-verification-record.md` to point to the artifact and
  runbook while preserving the no-go status until valid TLS and Minerva content
  are verified.
- Added `tests/test_public_site_artifact.py` to verify required Minerva
  positioning, docs/GitHub links, standalone static HTML constraints, and no
  rejected brand markers in `public-site/`.
- Test / Eval passed:
  `python3 -m compileall minerva_kernel` exited 0.
- Test / Eval passed:
  `python3 -m unittest discover -s tests` ran 97 tests and reported `OK`.
