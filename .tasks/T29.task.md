# Task: Add pack manifest examples and validation guidance

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/29
- Owner Lane: Docs / Ecosystem
- Allowed Files: `docs/`, `examples/`, `tests/`, `README.md`, `taxonomies/`, `policies/`, `models/`, `adapters/`

## Goal

Make taxonomy, policy, model, and adapter pack contributions concrete with
manifest examples and validation guidance.

## Acceptance Criteria

- Add example manifest files or documented JSON shapes for taxonomy, policy, model, and adapter packs.
- Include required fields, safety fields, ownership fields, and compatibility fields.
- Add lightweight validation tests if examples are machine-readable.
- Link from ecosystem contribution docs.
- Existing compile and unit tests pass.

## Non-Goals

- Do not publish packages.
- Do not create external accounts.
- Do not implement every adapter.
- Do not use VoxSign state, tasks, tools, or docs.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
```

## Output

Done.

- Added machine-readable taxonomy, policy, model, and adapter manifest examples
  under `examples/pack-manifests/`.
- Added validation guidance in `examples/pack-manifests/README.md` and linked it
  from `docs/ecosystem-contributions.md` and `examples/README.md`.
- Added `tests/test_pack_manifest_examples.py` to validate JSON manifests,
  required identity fields, ownership fields, compatibility fields, safety
  fields, type-specific sections, fail-closed policy behavior, and model eval
  safety requirements.
- Verified:
  - `python3 -m compileall minerva_kernel`
  - `python3 -m unittest discover -s tests`
