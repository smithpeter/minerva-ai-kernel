# Task: Add local provider health check command

- Status: pending
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/46
- Owner Lane: Kernel / Integration
- Allowed Files: `minerva_kernel/`, `tests/`, `docs/`, `README.md`, `.tasks/`

## Goal

Add a bounded health-check surface for optional local OpenAI-compatible
providers so users can distinguish baseline mode from provider-backed mode.

## Acceptance Criteria

- Add a CLI command or SDK helper that reports local provider reachability
  without requiring credentials or remote network access.
- Health output is structured and safe for CI logs.
- Tests cover reachable mock response, unreachable endpoint, and malformed
  response behavior.
- Docs explain that baseline mode remains the default no-provider path.

## Non-Goals

- Do not require Ollama, GPUs, Docker, or a remote model.
- Do not send secrets in health requests.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
```

## Output

Pending.
