# Task: Define Observation Schema v0

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/1
- Owner Lane: Kernel
- Allowed Files: `docs/`, `minerva_kernel/`, `tests/`

## Goal

Define the first stable Observation Schema for Minerva's local failure interpreter.

## Acceptance Criteria

- Observation schema includes command, cwd, exit_code, stdout_tail, stderr_tail, duration_ms, source, policy summary, and optional runtime metadata.
- Schema is documented in `docs/`.
- Python dataclass or typed structure exists in `minerva_kernel/`.
- Unit test validates minimal and recommended examples.

## Non-Goals

- Do not implement model inference.
- Do not add CI adapters.
- Do not touch VoxSign or unrelated repositories.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
```

## Output

Implemented Observation Schema v0.

- Added `Observation` dataclass fields for command, cwd, exit_code, stdout_tail, stderr_tail, duration_ms, source, policy_summary, and optional runtime metadata.
- Added `Observation.to_dict()` serialization with `schema_version: observation.v0`.
- Updated executor and planner to produce/consume v0 fields.
- Exported `Observation` from `minerva_kernel`.
- Documented the schema in `docs/observation-schema-v0.md`.
- Added unit tests for minimal and recommended examples in `tests/test_observation_schema.py`.
- Ran required eval: `python3 -m compileall minerva_kernel` passed.
- Additional validation: `python3 -m unittest tests.test_observation_schema` passed.
- Note: `python3 -m pytest tests/test_observation_schema.py tests/test_cli.py` could not run because `pytest` is not installed in this environment.
