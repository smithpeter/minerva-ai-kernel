# Task: Add CI markdown summary and run artifact design

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/16
- Owner Lane: Integration
- Allowed Files: `docs/`, `examples/`, `adapters/`, `tests/`, `minerva_kernel/`

## Goal

Design the next CI/CD integration surface: a concise markdown summary and JSON artifact shape for Minerva results.

## Acceptance Criteria

- Document a GitHub Actions integration pattern for `minerva observe --`.
- Add an example markdown summary output and JSON artifact shape.
- If runtime code is added, keep it deterministic and covered by tests.
- Include safety notes: no auto-repair, redaction before model input, policy-gated decisions.

## Non-Goals

- Do not require GitHub API credentials.
- Do not upload artifacts in real CI yet unless it can be done with static examples.
- Do not implement a hosted service.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
python3 -m minerva_kernel.eval_smoke
```

## Output

Completed.

- Added `docs/ci-integration-surface.md` with the GitHub Actions `minerva observe --` wrapper pattern, `$GITHUB_STEP_SUMMARY` markdown contract, `minerva_ci_run.v0` JSON artifact contract, and safety notes for no auto-repair, redaction before model input, and policy-gated decisions.
- Added static examples:
  - `examples/minerva-ci-summary.md`
  - `examples/minerva-ci-run-artifact.json`
- Linked the CI surface from `docs/integration-design.md` and added both examples to `examples/README.md`.
- Runtime code was not changed.
- Validation passed:
  - `python3 -m compileall minerva_kernel` -> passed (`Listing 'minerva_kernel'...`)
  - `python3 -m unittest discover -s tests` -> passed (`Ran 50 tests in 3.031s`, `OK`)
  - `python3 -m minerva_kernel.eval_smoke` -> passed (`Cases: 5/5 passed, 0 failed`)
