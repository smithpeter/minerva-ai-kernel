# Task: Implement deterministic eval report command

- Status: pending
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/27
- Owner Lane: Eval
- Allowed Files: `minerva_kernel/`, `tests/`, `evals/`, `docs/`, `README.md`

## Goal

Turn the current smoke eval and failure corpus state into a deterministic local
eval report command.

## Acceptance Criteria

- Add a command or module that emits M1 eval metrics as JSON and/or markdown from local fixtures.
- Include JSON validity, failure label accuracy, safe recovery decision rate, escalation quality, dangerous action rate, corpus counts, and gaps to target.
- Cover deterministic output with tests.
- Keep the command local, dependency-free, and free of model/network requirements.
- Existing compile, unit tests, and eval smoke pass.

## Non-Goals

- Do not train a model.
- Do not call remote models.
- Do not create a benchmark leaderboard.
- Do not use VoxSign state, tasks, tools, or docs.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
python3 -m minerva_kernel.eval_smoke
```

## Output

Pending.
