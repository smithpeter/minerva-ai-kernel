# Task: Add minervad diagnosis smoke script

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/62
- Owner Lane: Kernel / Daemon
- Allowed Files: `scripts/`, `tests/`, `docs/`, `.tasks/`

## Goal

Add a repeatable local smoke command for `minervad` `/health` and `/diagnose`
without requiring a long-running service outside the test process.

## Plan-Eng Review

### Scope Challenge
- Accepted scope: local smoke script that starts minervad on a random local
  port, posts a bounded observation, and verifies report-only output.
- Reduced or deferred scope: no service install, auth, or remote bind.

### What Already Exists
- Existing code/docs reused: `minerva_kernel.daemon`, daemon tests and docs.
- Existing tests/evals reused: daemon tests.

### NOT In Scope
- Systemd service management.
- Remote exposure.

### Architecture / Flow

```text
start local server
  -> GET /health
  -> POST /diagnose
  -> verify not_executed
  -> shutdown
```

### Failure Modes

| Failure mode | Covered by test/eval | Error handling | User-visible outcome |
| --- | --- | --- | --- |
| Port collision | yes | random port | smoke remains local |
| Diagnose executes action | yes | assertion fails | clear blocker |
| Server not shut down | yes | finally cleanup | no stale process |

### Test / Eval Plan
- Required command: `python3 -m unittest tests.test_minervad_smoke_script`
- Additional focused check: run script locally.

### Parallelization
- Can run after T55.
- Parallel lanes, if any: docs and smoke tests.

### Review Readiness
- Diff review focus: cleanup and localhost-only behavior.
- Release or docs impact: adds daemon release evidence command.

## Acceptance Criteria

- Add smoke script.
- Add tests.
- Document command.
- No critical Plan-Eng failure-mode gap remains open.

## Non-Goals

- Do not install services.
- Do not bind remote interfaces.

## Test / Eval

```bash
python3 -m unittest tests.test_minervad_smoke_script
```

## Output

Added `scripts/minervad-diagnose-smoke.py`, a local smoke command that starts
`minervad` on a random localhost port, checks `/health`, posts a bounded
`observation.v0` to `/diagnose`, verifies policy status and
`execution.state=not_executed`, then shuts the server down. Added tests for the
script function and direct script execution. Documented usage in minervad docs.

Verification:

```bash
python3 -m unittest tests.test_minervad_smoke_script tests.test_daemon
python3 scripts/minervad-diagnose-smoke.py
```
