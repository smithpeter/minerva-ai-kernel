# Task: Implement `minerva diagnose failure.json`

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/3
- Owner Lane: Kernel
- Allowed Files: `minerva_kernel/`, `tests/`, `docs/`, `examples/`

## Goal

Make the first user-facing diagnosis command work from a saved observation JSON file.

## Acceptance Criteria

- `minerva diagnose failure.json` reads an Observation Schema v0 JSON file.
- It builds a redacted prompt and obtains a `Decision` through the provider abstraction.
- It validates the decision with Policy Runtime v0.
- CLI output includes failure, action, confidence, risk, escalation, policy decision reason, and redaction summary when present.
- Tests cover successful diagnosis, invalid input, policy-blocked decisions, and redaction summary output.

## Non-Goals

- Do not implement command observation in this task.
- Do not execute any action selected by the model.
- Do not require a real local model in tests.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
```

## Output

Implemented `minerva diagnose failure.json` for Observation Schema v0 files.

- Added Observation JSON loading and validation via `Observation.from_dict`.
- Wired diagnose through redacted prompt construction, `LocalLLMRouter`/provider abstraction, and Policy Runtime v0 validation.
- CLI output now includes failure, action, confidence, risk, escalation, policy decision, policy decision reason, and redaction summary when present.
- Added CLI tests for successful diagnosis, invalid observation input, policy-blocked decisions, and redaction summary/redacted prompt behavior.
- Test / Eval passed:
  - `python3 -m compileall minerva_kernel`
  - `python3 -m unittest discover -s tests` (`35` tests)
