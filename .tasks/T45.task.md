# Task: Expand deterministic baseline signature coverage

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/45
- Owner Lane: Kernel / Eval
- Allowed Files: `minerva_kernel/`, `tests/`, `evals/`, `docs/`, `.tasks/`

## Goal

Improve the no-provider CPU-local baseline interpreter with more common failure
signatures while keeping decisions structured, low-risk, and policy-gated.

## Acceptance Criteria

- Add deterministic signatures for at least three additional common failure
  classes from the existing corpus.
- Add unit tests for each new signature.
- Keep provider-injected behavior unchanged.
- Update eval or docs if new labels affect published examples.

## Non-Goals

- Do not add remote LLM fallback.
- Do not introduce runtime dependencies.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
python3 -m minerva_kernel.eval_smoke
```

## Output

Completed deterministic baseline signature expansion.

- Added npm/package metadata failure detection.
- Added git repository failure detection.
- Added container build failure detection.
- Added port collision detection.
- Added unit tests for each new signature.
