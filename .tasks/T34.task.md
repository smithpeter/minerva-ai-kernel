# Task: Define local CPU model candidate registry and eval scoring contract

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/34
- Owner Lane: Eval / Research
- Allowed Files: `docs/`, `models/`, `evals/`, `tests/`, `README.md`, `CHANGELOG.md`

## Goal

Turn the sub-500M/CPU-local model research direction into a concrete candidate
registry and scoring contract.

## Acceptance Criteria

- Add a machine-readable or clearly structured registry for CPU-local model candidates, including parameter count, runtime path, quantization expectation, hardware target, license/provenance notes, and provider command.
- Add an eval scoring contract that covers JSON validity, failure label accuracy, safe recovery decision rate, escalation quality, dangerous action rate, latency, and fallback behavior.
- Link from model strategy, sub-500M plan, and current release/update notes where appropriate.
- Add lightweight tests if the registry is machine-readable.
- Existing compile and unit tests pass.

## Non-Goals

- Do not download models.
- Do not train or distill a model.
- Do not claim remote benchmark results.
- Do not use VoxSign state, tasks, tools, or docs.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
```

## Output

Added `models/cpu_model_candidates.json` with
`minerva.cpu_model_candidate_registry.v0`, four CPU-local/sub-500M candidates,
runtime paths, quantization expectations, CPU hardware targets,
license/provenance notes, and provider commands.

Added `docs/cpu-model-eval-scoring-contract.md` covering JSON validity, failure
label accuracy, safe recovery decision rate, escalation quality, dangerous
action rate, latency, and fallback behavior. Linked the registry and contract
from `docs/model-strategy.md`, `docs/sub-500m-cpu-model-plan.md`,
`evals/README.md`, `models/README.md`, `README.md`, and `CHANGELOG.md`.

Added `tests/test_cpu_model_candidates.py` to validate the machine-readable
registry contract and first eval candidate metadata.

Verification passed:

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
```

Unit result: 83 tests ran, OK.
