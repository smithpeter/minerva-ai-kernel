# Task: Add practical integration recipes for CI, SDK, and agents

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/33
- Owner Lane: Docs / Integration
- Allowed Files: `docs/`, `examples/`, `tests/`, `README.md`, `CHANGELOG.md`

## Goal

Make Minerva easy to integrate by showing concrete usage recipes that preserve
redaction, policy gating, and no-auto-repair defaults.

## Acceptance Criteria

- Add practical recipes for GitHub Actions, direct Python SDK usage, and an agent/tool-failure loop.
- Include copyable commands or minimal snippets that work with current code.
- Explain required inputs, outputs, logs/artifacts, and how users retrieve diagnostic value.
- State safety boundaries and expected failure modes for each recipe.
- Existing compile and unit tests pass.

## Non-Goals

- Do not publish packages.
- Do not add hosted service integration.
- Do not implement an auto-repair executor.
- Do not use VoxSign state, tasks, tools, or docs.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
```

## Output

Added `docs/integration-recipes.md` with practical recipes for GitHub Actions
CI diagnostics, direct Python SDK usage, and an agent tool-failure loop. Each
recipe includes required inputs, copyable commands or snippets, outputs and
diagnostic retrieval paths, safety boundaries, and expected failure modes.

Linked the recipes from `README.md` and `examples/README.md`.

Validation:

- `python3 -m compileall minerva_kernel` passed.
- `python3 -m unittest discover -s tests` passed: 80 tests OK.
