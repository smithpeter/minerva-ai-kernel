# Task: Add AI-team task seeding and timer runbook

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/48
- Owner Lane: Ops / AI Team
- Allowed Files: `scripts/`, `docs/`, `README.md`, `.tasks/`, `tests/`

## Goal

Make the local AI-team periodic task system easier to operate on Linux without
accidentally running outside the Minerva repository.

## Acceptance Criteria

- Document installing, checking, triggering, and disabling the systemd user
  timer.
- Document required environment variables and clean-worktree expectations.
- Add a small script or check that reports whether the timer is installed and
  whether pending tasks exist.
- Add tests where practical for any new script behavior.

## Non-Goals

- Do not modify VoxSign automation.
- Do not require root services.
- Do not bypass project boundary or contamination checks.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
```

## Output

Completed AI-team timer runbook and preflight.

- Added `scripts/ai-team-preflight.sh` for root/head, dirty state, pending task
  count, executor availability, systemd timer state, and readiness reason.
- Added `docs/ai-team-timer-runbook.md` with install, status, trigger, disable,
  preflight, and operating conditions.
- Linked preflight and runbook from README.
- Added unit coverage for preflight output fields.
