# Task: Add CLI execute-action usage examples and docs

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/44
- Owner Lane: Docs / Integration
- Allowed Files: `README.md`, `docs/`, `examples/`, `tests/`, `.tasks/`

## Goal

Make the explicit `minerva execute-action` path understandable without implying
automatic action execution.

## Acceptance Criteria

- Add a small example showing decision JSON plus optional source observation.
- Document that `observe` and SDK decisions remain advisory by default.
- Include policy-blocked and policy-allowed example outputs.
- Add or update docs tests if examples become executable fixtures.

## Non-Goals

- Do not change executor behavior.
- Do not add auto-repair or shell execution.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
```

## Output

Completed explicit execute-action usage documentation.

- Added `examples/execute-action-demo.md` with source observation, allowed
  `inspect_dependencies` decision, and policy-blocked escalation examples.
- Linked the demo from README, examples index, and integration recipes.
- Preserved the product boundary that `observe` and SDK decisions remain
  advisory by default.
