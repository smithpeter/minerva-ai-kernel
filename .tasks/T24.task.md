# Task: Add local model provider path and CPU model eval plan

- Status: pending
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/24
- Owner Lane: Kernel / Research
- Allowed Files: `docs/`, `examples/`, `minerva_kernel/`, `tests/`, `README.md`

## Goal

Make the next local model path concrete for Ollama/OpenAI-compatible providers
and sub-500M CPU/GGUF candidates.

## Acceptance Criteria

- Document the exact local provider setup path for at least one small candidate
  model.
- Document fallback behavior when no local provider is available.
- Define the first CPU model eval report shape, including JSON validity,
  dangerous action rate, escalation quality, and safe recovery decision rate.
- Keep remote models optional and clearly outside the minimum path.
- Existing compile, unit tests, and eval smoke pass.

## Non-Goals

- Do not ship model weights.
- Do not require GPU access.
- Do not start full distillation or training.
- Do not use VoxSign state, tasks, tools, or docs.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
python3 -m minerva_kernel.eval_smoke
```

## Output

Pending.
