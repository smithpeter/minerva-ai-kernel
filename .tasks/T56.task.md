# Task: Verify 7-day Minerva sandbox evidence (T52 deferred follow-up)

- Status: pending
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/63
- Owner Lane: Adoption / Integration (verify)
- Allowed Files: `pilot/`, `docs/`, `.tasks/`
- Earliest Run Date: 2026-05-18

## Goal

T52 shipped the sandbox repo + runbook + Minerva Action wiring on
2026-05-10, but its original acceptance required `pilot/sandbox-7day-
evidence.md` to list 7+ Actions runs across 7 consecutive UTC dates.
That requirement is **calendar-blocked** and cannot be satisfied in
the same session as setup. T56 finishes the verification once the
sandbox has actually been running long enough.

## Context

This task is the **deferred-time** half of T52. The original T52 split
into:
- T52 (done day-1): setup, runbook, day-1 evidence + at-least-one-fail
- T56 (deferred, this card): seven-day evidence audit

## Acceptance Criteria

- Earliest run date for this task is 2026-05-18 (≥ 7 days after T52
  setup on 2026-05-10).
- `pilot/sandbox-7day-evidence.md` lists 7+ qualifying Actions runs
  across 7 consecutive UTC dates from the
  `smithpeter/minerva-pilot-sandbox` repo.
- Each run row has: date (UTC), run URL, summary-present flag,
  artifact-present flag, link to Minerva CI markdown summary.
- At least one of the 7+ runs is the real-failure run referenced in
  T52.
- If the 7-day stretch has gaps (no run on a given UTC date), record
  the gap row honestly — do not skip dates or back-fill fake runs.

## Non-Goals

- Do not modify the sandbox repo's content to manufacture failing runs.
- Do not synthesise runs that didn't actually happen on those dates.
- Do not auto-execute model-selected actions inside the sandbox.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
# plus: manual cross-check that 7 listed runs exist in the sandbox
# repo's Actions tab on 7 distinct UTC dates.
```

## Output

(populated by AI team after 2026-05-18 evidence audit)

## Source of split

- Original T52 acceptance criterion: 7-day evidence in single task
- Split decision (2026-05-11): T52 worker was stuck in_progress for
  hours re-checking impossible acceptance; calendar-blocked criteria
  need a deferred task with an Earliest-Run-Date, not a single
  worker retry loop.
- See also: `.claude/rules/multi-agent.md` "Stuck external-worker
  rescue" rule.
