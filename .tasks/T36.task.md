# Task: Rerun full local release dry-run after package metadata fix

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/36
- Owner Lane: Release
- Allowed Files: `docs/`, `README.md`, `CHANGELOG.md`, `.minerva/release-evidence/`, `.tasks/`

## Goal

Rerun the full local release dry-run on the post-package-metadata-fix commit and
record fresh evidence that the documented editable install and `minerva` console
command path now work.

## Acceptance Criteria

- Add a new dated release evidence document for the current commit after the package metadata fix.
- Verify fresh virtualenv install using the documented no-index/no-deps/no-build-isolation editable install command and `minerva doctor` through the console command.
- Cover `minerva observe --`, run record inspection, `policy-check`, eval smoke, compile, unit tests, destructive-intent policy block, and redaction check.
- Update release readiness, dry-run guide, and changelog with the new evidence link and current status.
- Existing compile and unit tests pass.

## Non-Goals

- Do not publish a release.
- Do not publish packages.
- Do not download dependencies.
- Do not download models.
- Do not create external accounts.
- Do not use VoxSign state, tasks, tools, or docs.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
```

## Output

Completed the post-package-metadata-fix local release dry run for commit
`87dd7730a07f29c1b82de2ed937ad35c94db6e04`.

- Added fresh evidence:
  `.minerva/release-evidence/2026-05-08-post-metadata-fix-local-release-dry-run.md`.
- Verified a fresh Python 3.11 virtualenv install with
  `python -m pip install --no-index --no-deps --no-build-isolation -e .`.
- Verified the installed console command path:
  `command -v minerva` resolved to `.venv-t36-py311/bin/minerva`, and
  `minerva doctor` printed `Minerva doctor: repository skeleton is ready.`
- Confirmed installed distribution metadata exposes only `minerva_kernel` as a
  top-level package.
- Covered `minerva observe --`, run record inspection, saved decision
  `policy-check`, eval smoke, destructive-intent policy block, and redaction
  before persisted run records.
- Demo run record:
  `.minerva/runs/2026-05-08T072516Z-70f465d5.json`.
- Redaction run record:
  `.minerva/runs/2026-05-08T072606Z-8a11d895.json`.
- Updated `docs/m0-release-readiness.md`, `docs/m0-local-release-dry-run.md`,
  and `CHANGELOG.md` with the new evidence link and current status.
- Noted a release-environment caveat: this host's Python 3.14 and Python 3.13
  fresh virtualenvs do not seed `setuptools`, so the no-build-isolation install
  fails before package metadata evaluation unless the local build backend is
  present.

Verification passed:

- `python3 -m compileall minerva_kernel` -> passed.
- `python3 -m unittest discover -s tests` -> 83 tests OK.
- `python3 -m minerva_kernel.eval_smoke` -> 5/5 cases passed, 0 failed.
