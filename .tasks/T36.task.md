# Task: Rerun full local release dry-run after package metadata fix

- Status: pending
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/36
- Owner Lane: Release
- Allowed Files: `docs/`, `README.md`, `CHANGELOG.md`, `.minerva/release-evidence/`, `.tasks/`

## Goal

Rerun the full local release dry-run on the post-package-metadata-fix commit and
record fresh evidence that the documented editable install and `minerva` console
command path now work.

## Acceptance Criteria

- Add a new dated release evidence document for the current commit after the package metadata fix.
- Verify fresh virtualenv install using the documented no-index/no-deps/no-build-isolation editable install command and `minerva doctor` through the console command.
- Cover `minerva observe --`, run record inspection, `policy-check`, eval smoke, compile, unit tests, destructive-intent policy block, and redaction check.
- Update release readiness, dry-run guide, and changelog with the new evidence link and current status.
- Existing compile and unit tests pass.

## Non-Goals

- Do not publish a release.
- Do not publish packages.
- Do not download dependencies.
- Do not download models.
- Do not create external accounts.
- Do not use VoxSign state, tasks, tools, or docs.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
```

## Output

Pending.
