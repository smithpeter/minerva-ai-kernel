# Task: Add release handoff inventory artifact

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/59
- Owner Lane: Release / Ops
- Allowed Files: `scripts/`, `tests/`, `docs/`, `README.md`, `.tasks/`

## Goal

Give the release owner a bounded local handoff artifact that explains current
dashboard status, dirty-worktree scope, changed-file categories, and immediate
release blockers without calling external services.

## Plan-Eng Review

### Scope Challenge
- Accepted scope: local-only script and tests that summarize existing release
  dashboard output and git status.
- Reduced or deferred scope: no commit creation, PR publishing, GitHub API, or
  remote CI/domain verification.

### What Already Exists
- Existing code/docs reused: `scripts/release-ops-dashboard.py`, release
  readiness docs, `.tasks/board.md`.
- Existing tests/evals reused: release ops dashboard tests.

### NOT In Scope
- Publishing or deploying.
- Hiding dirty worktree blockers.
- Inspecting file contents for secrets.

### Architecture / Flow

```text
release ops dashboard
  -> git status porcelain
  -> bounded category summary
  -> release_handoff.v0
```

### State / Decision Diagram

```text
[dashboard ready] -> [clean worktree] -> [ready handoff]
        |                  |
        v                  v
 [dashboard fail]    [dirty worktree]
        |                  |
        v                  v
 [blocked summary] [changed file inventory]
```

### Failure Modes

| Failure mode | Covered by test/eval | Error handling | User-visible outcome |
| --- | --- | --- | --- |
| Dashboard reports blockers | yes | artifact marks not ready | clear blocker list |
| Git status fails | yes | artifact marks failed check | clear error |
| Output leaks file contents | yes | only paths/status/category emitted | bounded output |

### Test / Eval Plan
- Required command: `python3 -m unittest tests.test_release_handoff`
- Additional focused check: run script locally with `--json`.

### Parallelization
- Sequential because it consumes T57 dashboard output.
- Parallel lanes, if any: docs can be reviewed separately.

### Review Readiness
- Diff review focus: local-only behavior, bounded output, no content dumps.
- Release or docs impact: release handoff becomes the next action after the
  dashboard.

## Acceptance Criteria

- Add a local release handoff artifact command.
- Include dashboard readiness, dirty-worktree inventory, and file category
  counts.
- Add tests for clean, dirty, and command-failure paths.
- Document the command near release operations docs.
- No critical Plan-Eng failure-mode gap remains open.

## Non-Goals

- Do not commit changes.
- Do not call external services.
- Do not publish release artifacts.

## Test / Eval

```bash
python3 -m unittest tests.test_release_handoff
```

## Output

Added `scripts/release-handoff.py`, a local-only handoff artifact generator that
wraps `release_ops_status.v0`, reads `git status --porcelain`, categorizes
changed paths, reports blockers, and emits `minerva.release_handoff.v0`.
Added tests for clean handoff, dirty inventory, dashboard blocker reporting,
and git-status failure. Documented usage in README and release readiness docs.

Verification:

```bash
python3 -m unittest tests.test_release_handoff tests.test_release_ops_dashboard
python3 scripts/release-handoff.py --json --no-fail --timeout 120
```
