# Task: Automate GitHub Actions and domain readiness checks

- Status: pending
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/32
- Owner Lane: Release / Integration
- Allowed Files: `docs/`, `scripts/`, `tests/`, `README.md`, `CHANGELOG.md`

## Goal

Make release readiness checks for CI and `minervakernel.com` repeatable and
evidence-oriented.

## Acceptance Criteria

- Add a script or documented command checklist that checks GitHub Actions status for the current commit and DNS/TLS/HTTPS readiness for `minervakernel.com`.
- Record what can be verified locally versus what requires external permissions or network availability.
- Update the CI/domain verification record with the new repeatable process.
- Keep failure output bounded and safe for public issue comments.
- Existing compile and unit tests pass.

## Non-Goals

- Do not mutate DNS.
- Do not deploy a website.
- Do not handle credentials.
- Do not use VoxSign state, tasks, tools, or docs.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
```

## Output

Pending.
