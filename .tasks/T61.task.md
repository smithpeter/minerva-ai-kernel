# T61: Collect first 25 verification cases

Status: done

Owner lane:

```text
Verify / Eval
```

GitHub issue:

```text
TBD
```

## Goal

Create the first verification corpus slice for Minerva Verify using real or
realistic PR/code-review cases.

## Acceptance Criteria

- At least 25 cases.
- Each case includes diff summary, expected risk, expected missing evidence,
  recommended verification command, and outcome when available.
- Cases include docs-only, runtime code, tests, dependency changes, policy
  changes, schema changes, and CI failure examples.
- Eval report measures useful merge evidence rate.

## Completed Outputs

- Added `evals/verify_cases_v0.json` with 25 cases.
- Added `minerva_kernel.verify_eval`.
- Added `minerva verify-eval-report`.
- Added tests for fixture validity, metric output, markdown rendering, and CLI.

## Verification

```bash
python3 -m unittest discover -s tests
```
