# Task: Add local model benchmark evidence gate

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/61
- Owner Lane: Research / Model
- Allowed Files: `scripts/`, `tests/`, `docs/`, `evals/`, `models/`, `.tasks/`

## Goal

Add a local-only gate that distinguishes fixture CPU eval promotion from real
local model benchmark evidence.

## Plan-Eng Review

### Scope Challenge
- Accepted scope: validate benchmark evidence artifacts and fail clearly when
  only example or fixture evidence is present.
- Reduced or deferred scope: no model download and no benchmark execution
  against a missing local provider.

### What Already Exists
- Existing code/docs reused: CPU eval reports, local CPU model runbook,
  candidate registry.
- Existing tests/evals reused: CPU model candidate tests.

### NOT In Scope
- Downloading weights.
- Claiming benchmark results without real evidence.

### Architecture / Flow

```text
benchmark artifact
  -> schema/evidence validator
  -> release gate status
```

### Failure Modes

| Failure mode | Covered by test/eval | Error handling | User-visible outcome |
| --- | --- | --- | --- |
| Example artifact treated as benchmark | yes | validation fails | clear blocker |
| Missing latency/eval evidence | yes | validation fails | clear blocker |
| Remote fallback evidence present | yes | validation fails | clear blocker |

### Test / Eval Plan
- Required command: `python3 -m unittest tests.test_cpu_model_benchmark_gate`
- Additional focused check: run gate against example artifact.

### Parallelization
- Can run after T53.
- Parallel lanes, if any: validator and docs.

### Review Readiness
- Diff review focus: no benchmark overclaim.
- Release or docs impact: strengthens CPU model claims.

## Acceptance Criteria

- Add benchmark evidence validator or script.
- Add tests for fixture/example rejection and valid synthetic artifact.
- Document release gate use.
- No critical Plan-Eng failure-mode gap remains open.

## Non-Goals

- Do not download models.
- Do not fabricate benchmark results.

## Test / Eval

```bash
python3 -m unittest tests.test_cpu_model_benchmark_gate
```

## Output

Added `scripts/check-cpu-benchmark-evidence.py`, a local benchmark evidence
gate that rejects example/fixture artifacts and validates real local provider
artifacts for embedded `minerva.cpu_model_eval_report.v0`, minimum case count,
required metrics, non-fixture runtime, and minimum-path flags. Added tests for
example rejection, valid synthetic artifact acceptance, fixture runtime
rejection, and CLI nonzero behavior. Documented the gate in the local CPU model
runbook and model README.

Verification:

```bash
python3 -m unittest tests.test_cpu_model_benchmark_gate tests.test_cpu_model_candidates
python3 scripts/check-cpu-benchmark-evidence.py evals/cpu_model_report_artifact.example.json; test "$?" -eq 1
```
