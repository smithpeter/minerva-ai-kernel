# Task: Expand CPU eval corpus to contract-ready coverage

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/51
- Owner Lane: Eval
- Allowed Files: `evals/`, `tests/`, `docs/`, `.tasks/`

## Goal

Grow `evals/cpu_model_cases.jsonl` from the current tiny fixture into a
contract-ready CPU controller corpus that better represents Python, shell,
network, permissions, service, ambiguous/risky, and fallback behavior.

## Plan-Eng Review

### Scope Challenge
- Accepted scope: add a focused, validated corpus expansion with expected
  failure/action/escalation/safe-recovery fields and fixture decisions.
- Reduced or deferred scope: do not run a real model or claim benchmark
  promotion; this is corpus and regression coverage only.

### What Already Exists
- Existing code/docs reused: `minerva_kernel.cpu_model_eval`,
  `docs/cpu-model-eval-scoring-contract.md`, and
  `evals/cpu_model_cases.jsonl`.
- Existing tests/evals reused: `tests/test_cpu_model_eval.py` and
  `python3 -m minerva_kernel.cpu_model_eval`.

### NOT In Scope
- Training or distillation.
- Downloading models.
- Large generated corpora without review.

### Architecture / Flow

```text
new JSONL cases
  -> CPUModelEvalCase validation
  -> fixture decision scoring
  -> report metrics by category/fallback
```

### State / Decision Diagram

```text
[draft case] -> [schema valid] -> [fixture scored] -> [corpus accepted]
      |              |                 |
      v              v                 v
[duplicate id]  [bad expected]   [metric regression]
      |              |                 |
      v              v                 v
   [reject]       [reject]          [fix case]
```

### Failure Modes

| Failure mode | Covered by test/eval | Error handling | User-visible outcome |
| --- | --- | --- | --- |
| Duplicate or malformed eval case | yes | loader/test failure | clear failure |
| Safe-recovery eligibility mislabeled | yes | metric expectation failure | clear failure |
| Fallback case accidentally calls remote path | yes | report fallback metrics | clear failure |

### Test / Eval Plan
- Required command: `python3 -m unittest tests.test_cpu_model_eval`
- Additional focused check: `python3 -m minerva_kernel.cpu_model_eval`

### Parallelization
- Sequential because corpus edits and metric expectations share the same files.
- Parallel lanes, if any: none.

### Review Readiness
- Diff review focus: case quality, category balance, exact expected labels, and
  no unsafe actions in fixture decisions.
- Release or docs impact: eval README and CPU scoring contract may need count
  updates if thresholds or corpus shape change.

## Acceptance Criteria

- Expand CPU eval cases to at least 30 reviewed cases.
- Cover every `CASE_MIX_ORDER` category with at least 3 cases.
- Include at least 2 expected fallback cases.
- Tests assert corpus count, category minimums, and fallback coverage.
- No critical Plan-Eng failure-mode gap remains open.

## Non-Goals

- Do not download or run real models.
- Do not modify provider implementations unless required by validation.
- Do not weaken promotion gates.

## Test / Eval

```bash
python3 -m unittest tests.test_cpu_model_eval
python3 -m minerva_kernel.cpu_model_eval
```

## Output

Completed CPU eval corpus expansion.

- Expanded `evals/cpu_model_cases.jsonl` from 2 to 30 reviewed fixture cases.
- Covered all CPU eval categories with 5 cases each: `python`, `shell`,
  `network`, `permissions`, `service`, and `ambiguous_or_risky`.
- Added 2 expected fallback cases for local provider unavailability.
- Added CPU eval tests for corpus size, duplicate IDs, category minimums,
  fallback coverage, and safe-recovery eligibility.
- Verified fixture report has 30/30 valid decisions, zero dangerous actions,
  17/17 safe recovery decisions, and 2/2 successful fallbacks.
