# Task: Add AI-team stabilization exit criteria

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/64
- Owner Lane: Ops / AI Team
- Allowed Files: `docs/`, `scripts/`, `tests/`, `.tasks/`

## Goal

Define when the AI-team loop should stop adding scope and switch to release
stabilization, verification, and handoff.

## Plan-Eng Review

### Scope Challenge
- Accepted scope: docs and checks for stabilization criteria.
- Reduced or deferred scope: no automatic git commit or PR publishing.

### What Already Exists
- Existing code/docs reused: AI-team workflow, release dashboard, task board.
- Existing tests/evals reused: Plan-Eng workflow tests.

### NOT In Scope
- Stopping user-directed work.
- Publishing releases.

### Architecture / Flow

```text
task queue + dashboard + tests
  -> stabilization criteria
  -> release handoff
```

### Failure Modes

| Failure mode | Covered by test/eval | Error handling | User-visible outcome |
| --- | --- | --- | --- |
| Infinite scope growth | yes | criteria says stabilize | clear phase change |
| Pending blockers ignored | yes | dashboard remains not_ready | clear blocker |

### Test / Eval Plan
- Required command: `bash scripts/check-plan-eng-review.sh`
- Additional focused check: docs test if added.

### Parallelization
- Should run after T59.
- Parallel lanes, if any: none.

### Review Readiness
- Diff review focus: criteria are concrete and measurable.
- Release or docs impact: prevents endless unreviewed expansion.

## Acceptance Criteria

- Add stabilization criteria.
- Connect criteria to dashboard and task queue.
- Add or update tests/checks where practical.
- No critical Plan-Eng failure-mode gap remains open.

## Non-Goals

- Do not block explicit future user requests.
- Do not publish releases automatically.

## Test / Eval

```bash
bash scripts/check-plan-eng-review.sh
```

## Output

Added `docs/ai-team-stabilization.md` and
`scripts/check-ai-team-stabilization.py` to define and check when the AI-team
loop should stop expanding scope and move to release handoff. Linked the
criteria from AI-team execution docs. Added tests for ready, pending-task, and
missing-required-file states.

Verification:

```bash
python3 -m unittest tests.test_ai_team_stabilization tests.test_plan_eng_review_workflow
python3 scripts/check-ai-team-stabilization.py; test "$?" -eq 1
```

The pre-completion check intentionally reported T64 as pending; after this
task is marked done, the same script should report `ready=true` unless a new
pending task is added.
