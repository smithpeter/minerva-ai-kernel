# Task: Add CI renderer fixture and artifact contract regression coverage

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/22
- Owner Lane: Integration
- Allowed Files: `minerva_kernel/`, `tests/`, `docs/`, `examples/`

## Goal

Protect the `minerva_ci_run.v0` artifact and markdown summary contracts before
M1 integrations depend on them.

## Acceptance Criteria

- Add stable fixtures or tests for happy path, policy-blocked path, and
  redaction propagation.
- Assert the artifact schema version and required top-level fields.
- Assert markdown summaries stay bounded and do not include credential-shaped
  values.
- Document any intentional artifact contract change in
  `docs/ci-integration-surface.md`.
- Existing compile, unit tests, and eval smoke pass.

## Non-Goals

- Do not upload artifacts to CI.
- Do not add a hosted service.
- Do not call the GitHub API.
- Do not use VoxSign state, tasks, tools, or docs.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
python3 -m minerva_kernel.eval_smoke
```

## Output

Added CI renderer regression coverage in `tests/test_ci_render.py` for:

- stable artifact contract fields across happy path, policy-blocked path, and
  redaction propagation cases;
- `minerva_ci_run.v0` schema version and required top-level, summary, and
  safety fields;
- bounded markdown summaries that exclude credential-shaped raw values while
  preserving redaction markers.

No artifact contract shape changed, so `docs/ci-integration-surface.md` did not
need an update.

Validation:

- `python3 -m compileall minerva_kernel` passed.
- `python3 -m unittest discover -s tests` passed: 57 tests.
- `python3 -m minerva_kernel.eval_smoke` passed: 5/5 cases.
