# Task: Grow failure corpus and publish M1 eval metrics

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/25
- Owner Lane: Eval
- Allowed Files: `evals/`, `tests/`, `docs/`, `README.md`

## Goal

Move the failure corpus beyond the M0 synthetic slice and make M1 quality
measurable.

## Acceptance Criteria

- Grow each required corpus category toward the next documented target.
- Preserve duplicate-ID checks, schema validation, and read-only safe actions.
- Add or update an eval report that includes JSON validity, failure label
  accuracy, safe recovery decision rate, escalation quality, and dangerous
  action rate.
- Document remaining corpus gaps and risky/ambiguous case review needs.
- Existing compile, unit tests, and eval smoke pass.

## Non-Goals

- Do not include secrets, private logs, full environment dumps, proprietary
  code, or customer data.
- Do not train a model in this task.
- Do not use VoxSign state, tasks, tools, or docs.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
python3 -m minerva_kernel.eval_smoke
```

## Output

Completed M1 eval corpus/report update.

- Confirmed git root: `/Users/zouyongming/projects/minerva-ai-kernel`.
- Expanded `evals/failure_cases_m0_slice.json` from 100 to 112 validated cases.
- Added one new synthetic, redaction-safe case for each required corpus category.
- Current category counts: python 9, shell-cli 9, git 10, npm-node 9,
  docker-build 9, dns-network 9, permission 9, timeout 9, ci 10,
  model-api 10, schema-json 10, secret-redaction 9.
- Added `evals/m1_eval_report.md` with JSON validity, failure label accuracy,
  safe recovery decision rate, escalation quality, dangerous action rate,
  remaining corpus gaps, and risky/ambiguous review needs.
- Updated `evals/README.md` and `tests/test_failure_corpus.py` for the M1
  corpus floor.
- Verification passed:
  - `python3 -m compileall minerva_kernel`
  - `python3 -m unittest discover -s tests` (61 tests)
  - `python3 -m minerva_kernel.eval_smoke` (5/5 passed, 0 failed)
