# Task: Add GitHub Pages deployment workflow for public site

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/42
- Owner Lane: Release / Integration
- Allowed Files: `.github/workflows/`, `public-site/`, `docs/`, `README.md`, `CHANGELOG.md`, `tests/`, `.tasks/`

## Goal

Make the Minerva public site artifact deployable without depending on the
currently contaminated server at `49.51.134.101`.

## Context

Current readiness for commit `f5b333275e639ea502dbba328e8e3758323c3b46` is
green for local install backend, GitHub Actions, and DNS, but not ready because
`minervakernel.com` still serves a certificate/page from VoxSign. T41 added
`public-site/index.html` and a cutover runbook. The next repo-side step is to
add a GitHub Pages deployment path for `public-site/`.

## Acceptance Criteria

- Add a GitHub Actions workflow or documented Pages deployment path for
  `public-site/`.
- Include the custom-domain artifact if appropriate, but keep docs clear that
  DNS/custom-domain settings must still be verified before claiming readiness.
- Add tests or workflow assertions so the Pages workflow publishes `public-site`
  and does not reference VoxSign.
- Update docs/changelog/readiness with the Pages deployment path and remaining
  external DNS/TLS requirements.
- Do not deploy to the existing VoxSign server.

## Non-Goals

- Do not modify VoxSign files or workflows.
- Do not claim `minervakernel.com` is ready until release readiness passes.
- Do not require private server credentials.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
```

## Output

Implemented GitHub Pages deployment path for `public-site/`.

- Added `.github/workflows/pages.yml` to validate the static artifact, upload
  `public-site/`, and deploy with GitHub Pages actions.
- Added `public-site/CNAME` for `minervakernel.com`; docs state that Pages
  custom-domain settings, DNS, TLS, HTTPS content, and brand-contamination
  checks still require release-owner verification.
- Added `tests/test_pages_workflow.py` assertions for public-site publishing,
  Pages permissions, no private-server deployment commands, no VoxSign
  references in the workflow, and the custom-domain artifact.
- Updated README, changelog, release readiness, CI/domain verification, and
  domain cutover runbook with the Pages path and remaining external
  requirements.
- Eval passed:
  - `python3 -m compileall minerva_kernel`
  - `python3 -m unittest discover -s tests` ran 101 tests, OK.
