# Task: Add CI and domain verification record

- Status: pending
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/19
- Owner Lane: Release
- Allowed Files: `docs/`, `examples/`, `README.md`

## Goal

Record the pre-launch verification process for GitHub Actions and
`minervakernel.com` without requiring secret access.

## Acceptance Criteria

- Add a document or example record for CI status, domain DNS/TLS target, and launch blocker verification.
- Include commands that can be run locally where possible.
- Clearly separate verified facts from release-owner manual confirmations.
- Link from release readiness.
- Existing compile and unittest pass.

## Non-Goals

- Do not change DNS.
- Do not deploy any website.
- Do not create external accounts.
- Do not require secret access.
- Do not change runtime code.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
```

## Output

Pending.
