# Task: Add local release dry-run guide and command checklist

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/18
- Owner Lane: Release
- Allowed Files: `docs/`, `examples/`, `README.md`

## Goal

Make the M0 release dry run repeatable from a clean checkout.

## Acceptance Criteria

- Add a release dry-run guide or checklist that walks through local install, `minerva doctor`, observe demo, run record inspection, eval smoke, and safety checks.
- Include exact commands and expected artifacts.
- Link from release readiness and README.
- Existing compile and unittest pass.

## Non-Goals

- Do not publish a release.
- Do not create social posts or external accounts.
- Do not deploy a hosted site.
- Do not change runtime code.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
```

## Output

- Added `docs/m0-local-release-dry-run.md` with a clean-checkout local install,
  `minerva doctor`, observe demo, run record inspection, saved decision
  `policy-check`, eval smoke, destructive-command blocking, redaction check,
  final compile/unit/eval gate, and expected local artifacts.
- Linked the dry-run guide from `README.md` and
  `docs/m0-release-readiness.md`.
- Ran `python3 -m compileall minerva_kernel`: passed.
- Ran `python3 -m unittest discover -s tests`: passed, 54 tests OK.
