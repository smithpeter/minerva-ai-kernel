# Task: Record full local release dry-run evidence for current commit

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/31
- Owner Lane: Release
- Allowed Files: `docs/`, `README.md`, `CHANGELOG.md`, `.minerva/release-evidence/`

## Goal

Run and record the full local release dry-run evidence for the current
release/announcement commit.

## Acceptance Criteria

- Add a dated release dry-run evidence document with exact commands and observed outputs.
- Cover install/smoke path, `minerva doctor`, `minerva observe --`, run record inspection, `policy-check`, eval smoke, compile, and unit tests.
- Record commit SHA, machine context, date, and any known gaps or blockers.
- Link from release readiness, dry-run guide, and changelog where appropriate.
- Existing compile and unit tests pass.

## Non-Goals

- Do not publish a release.
- Do not create external accounts.
- Do not run destructive commands.
- Do not use VoxSign state, tasks, tools, or docs.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
```

## Output

Git root confirmed: `/Users/zouyongming/projects/minerva-ai-kernel`.

Added `.minerva/release-evidence/2026-05-08-local-release-dry-run.md` for
commit `ae8bc6bd773f9b745f5b8bbcb8c7b24518882ac7`. The evidence records machine
context, exact commands and observed outputs for install/smoke, doctor, observe,
run-record inspection, policy-check, eval smoke, destructive-intent policy
blocking, redaction, compile, and unit tests.

Linked the evidence from `docs/m0-release-readiness.md`,
`docs/m0-local-release-dry-run.md`, and `CHANGELOG.md`.

Results:

- `python3 -m compileall minerva_kernel` passed.
- `python3 -m unittest discover -s tests` passed: 77 tests, OK.
- `python3 -m minerva_kernel.eval_smoke` passed: 5/5 cases, 0 failed.
- Demo run record: `.minerva/runs/2026-05-08T064103Z-ba79c294.json`.
- Redaction run record: `.minerva/runs/2026-05-08T064207Z-4d5cf3d7.json`.
- Known blocker recorded: editable install currently fails package metadata
  discovery, so the `minerva` console script was unavailable; CLI behavior was
  verified through `python3 -m minerva_kernel.cli ...`.
