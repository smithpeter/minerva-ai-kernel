# Task: Seed next roadmap tasks after M0 readiness

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/20
- Owner Lane: Planning
- Allowed Files: `docs/`, `.tasks/`, `README.md`

## Goal

Convert current M0 completion into a concrete next task queue for M1.

## Acceptance Criteria

- Add a concise M1 roadmap/task seed document covering CI renderers, integrations, local model path, eval growth, and ecosystem docs.
- Add suggested T21+ task cards or GitHub issue plan if appropriate.
- Preserve VoxSign isolation and Minerva-specific ownership boundaries.
- Existing docs remain consistent.

## Non-Goals

- Do not implement runtime features unless needed for documentation references.
- Do not create social posts or external accounts.
- Do not use VoxSign state, tasks, tools, or docs.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
```

## Output

Completed.

- Added `docs/m1-roadmap-task-seeds.md` with a concise M1 plan covering CI
  renderers, integrations, local model path, eval growth, and ecosystem docs.
- Added suggested local task cards:
  - `.tasks/T21.task.md`
  - `.tasks/T22.task.md`
  - `.tasks/T23.task.md`
  - `.tasks/T24.task.md`
  - `.tasks/T25.task.md`
  - `.tasks/T26.task.md`
- Updated `.tasks/board.md` with T21-T26 as pending proposed M1 tasks and set
  T20 to done.
- Updated `docs/github-issues.md` with the M0 readiness continuation and the
  suggested M1 GitHub issue queue.
- Linked the M1 roadmap task seeds from `README.md`.
- Preserved VoxSign isolation by keeping all edits inside allowed Minerva docs
  and task files and by adding explicit no-VoxSign boundaries to the M1 plan
  and T21-T26 cards.
- Runtime code was not changed.
- Validation passed:
  - `python3 -m compileall minerva_kernel` -> passed (`Listing 'minerva_kernel'...`)
  - `python3 -m unittest discover -s tests` -> passed (`Ran 54 tests in 2.910s`, `OK`)
