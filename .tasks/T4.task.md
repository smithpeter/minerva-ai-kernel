# Task: Implement redaction v0 before model input

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/6
- Owner Lane: Security
- Allowed Files: `minerva_kernel/`, `tests/`, `docs/`

## Goal

Add basic redaction so logs and observations can be processed without exposing obvious secrets to model providers or saved records.

## Acceptance Criteria

- Redacts bearer tokens, common API key patterns, passwords, private key blocks, GitHub tokens, and cloud credential-like strings.
- Decision/run record includes redaction count and types.
- Tests cover each redaction category.
- Docs explain what is and is not collected by default.

## Non-Goals

- Do not build a full DLP system.
- Do not collect full environment variables by default.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
```

## Output

Implemented redaction v0 in `minerva_kernel/redaction.py` with coverage for bearer tokens, API key patterns, passwords, private key blocks, GitHub tokens, and cloud credential-like strings.

Wired redaction into model input via `build_prompt()`, and added redaction summaries to serialized observation and decision records when redactions occur.

Updated schema docs to explain default collection boundaries and redaction metadata.

Verification:

- `python3 -m unittest discover -s tests` passed: 27 tests.
- `python3 -m compileall minerva_kernel` passed.
