# Task: Fix CI smoke failure caused by Python 3.x selecting 3.14

- Status: pending
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/39
- Owner Lane: Integration / Release
- Allowed Files: `.github/workflows/`, `tests/`, `docs/`, `README.md`, `CHANGELOG.md`, `.tasks/`

## Goal

Restore a green GitHub Actions smoke gate for the current release branch by
removing the unstable `3.x` Python selection that picked Python 3.14.4 and broke
the offline editable-install smoke test.

## Evidence

Readiness check for commit `f65835f77ebc6674c812805dd377ef2cf2706b70`
reported CI failure in run `25544320298`. The Minerva CI artifact showed
`tests/test_install_entrypoint.py` failed because the GitHub runner used Python
3.14.4 and the fresh venv/local site-packages did not provide `setuptools>=77`
for the no-build-isolation editable install test.

## Acceptance Criteria

- Update CI so the smoke gate runs on an explicit supported Python version
  aligned with current release evidence, instead of floating `3.x`.
- Preserve the install smoke test intent: the documented `minerva` console
  command must still be verified.
- Update docs/changelog if the supported CI Python version is part of release
  readiness.
- Run compile and unit tests locally.
- Trigger or prepare a new CI run so readiness can be checked again.

## Non-Goals

- Do not download dependencies in local tests.
- Do not remove the install entrypoint smoke test.
- Do not touch VoxSign files or workflows.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
```

## Output

Pending.
