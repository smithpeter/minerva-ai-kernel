# Task: Add install and CLI entry point smoke tests

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/13
- Owner Lane: Integration
- Allowed Files: `tests/`, `docs/`, `README.md`, `pyproject.toml`, `.github/`

## Goal

Verify that the package can be installed locally and the `minerva` console command works as documented.

## Acceptance Criteria

- Add a test or script that validates `python -m minerva_kernel.cli` or installed `minerva` entry behavior.
- README quickstart includes local install command and first `minerva doctor`, `minerva observe --`, and `python -m minerva_kernel.eval_smoke` commands.
- CI covers the install/entry smoke path if practical.
- Existing compile, unittest, and eval smoke still pass.

## Non-Goals

- Do not publish to PyPI.
- Do not add runtime dependencies unless strictly required.
- Do not require remote LLMs, GPUs, Ollama, Docker, or network access.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
python3 -m minerva_kernel.eval_smoke
```

## Output

- Added `tests/test_install_entrypoint.py` with smoke coverage for
  `python -m minerva_kernel.cli doctor` and a no-network temporary local wheel
  install that runs the installed `minerva doctor` console script.
- Added a README quickstart with local install, `minerva doctor`,
  `minerva observe --`, and `python -m minerva_kernel.eval_smoke` commands.
- Updated CI smoke wording so unittest discovery explicitly covers unit and
  install entry smoke tests.
- Verification passed:
  - `python3 -m compileall minerva_kernel`
  - `python3 -m unittest discover -s tests` (48 tests)
  - `python3 -m minerva_kernel.eval_smoke` (5/5 cases passed)
