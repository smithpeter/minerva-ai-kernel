# Task: Add adapter event implementation checklist

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/63
- Owner Lane: Docs / Ecosystem
- Allowed Files: `docs/`, `examples/`, `tests/`, `.tasks/`

## Goal

Turn the adapter event reporting guide into a contributor checklist that
external adapter authors can apply before publishing integrations.

## Plan-Eng Review

### Scope Challenge
- Accepted scope: checklist and docs validation.
- Reduced or deferred scope: no vendor-specific adapter implementation.

### What Already Exists
- Existing code/docs reused: adapter event reporting guide, pack manifest docs.
- Existing tests/evals reused: adapter event example tests.

### NOT In Scope
- Hosted API.
- Platform credentials.

### Architecture / Flow

```text
adapter idea
  -> checklist
  -> schema/policy/redaction validation
  -> publishable adapter docs
```

### Failure Modes

| Failure mode | Covered by test/eval | Error handling | User-visible outcome |
| --- | --- | --- | --- |
| Checklist omits policy gate | yes | docs test fails | clear fix |
| Checklist implies auto-repair | yes | docs review fails | clear fix |

### Test / Eval Plan
- Required command: `python3 -m unittest tests.test_adapter_event_examples`
- Additional focused check: grep checklist for required boundary terms.

### Parallelization
- Can run independently after T58.
- Parallel lanes, if any: none.

### Review Readiness
- Diff review focus: no auto-repair or credential requirements.
- Release or docs impact: ecosystem onboarding.

## Acceptance Criteria

- Add checklist.
- Link it from adapter docs.
- Validate required safety terms.
- No critical Plan-Eng failure-mode gap remains open.

## Non-Goals

- Do not implement a real adapter.

## Test / Eval

```bash
python3 -m unittest tests.test_adapter_event_examples
```

## Output

Added `docs/adapter-implementation-checklist.md` covering event mapping,
redaction, diagnosis surfaces, policy gate, report-only default, tests, and
release notes for external adapters. Linked it from the adapter event reporting
guide. Added tests that assert required safety boundaries and required adapter
test cases remain documented.

Verification:

```bash
python3 -m unittest tests.test_adapter_implementation_checklist tests.test_adapter_event_examples
rg -n "adapter_executes_actions=false|execution.state=not_executed|policy_decision.allowed|Redact before model input" docs/adapter-implementation-checklist.md docs/adapter-event-reporting.md
```
