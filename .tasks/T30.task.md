# Task: Draft release notes and public update for current milestone

- Status: done
- GitHub Issue: https://github.com/smithpeter/minerva-ai-kernel/issues/30
- Owner Lane: Release
- Allowed Files: `docs/`, `README.md`, `CHANGELOG.md`

## Goal

Prepare a concise release note/public update from the completed M0/M1 prep work.

## Acceptance Criteria

- Add a changelog or release notes draft summarizing M0 and M1-prep achievements.
- Include exact current capabilities, non-claims, safety boundaries, and next tasks.
- Link to dry-run, CI/domain verification, eval report, and ecosystem guide.
- Keep it suitable for GitHub release notes or a public project update.
- Existing compile and unit tests pass.

## Non-Goals

- Do not publish a release.
- Do not post to social channels.
- Do not create external accounts.
- Do not use VoxSign state, tasks, tools, or docs.

## Test / Eval

```bash
python3 -m compileall minerva_kernel
python3 -m unittest discover -s tests
```

## Output

Created `CHANGELOG.md` with a GitHub-release/public-update draft for the
2026-05-08 M0 / M1-prep milestone. The draft summarizes current CLI, CI, eval,
and ecosystem capabilities; states non-claims and safety boundaries; lists next
tasks; and links to the M0 dry-run guide, CI/domain verification record, M1 eval
report, ecosystem contribution guide, M1 roadmap, CI integration surface, release
readiness checklist, and launch draft.

Verification passed:

- `python3 -m compileall minerva_kernel` exited 0.
- `python3 -m unittest discover -s tests` ran 77 tests and passed with `OK`.
